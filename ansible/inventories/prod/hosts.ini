[managers]
# comma-separated list is also provided via GH secret for CI deploy step
mgr1 ansible_host=CHANGE_ME ansible_user={{ lookup('env','SSH_USER') }}

[all:vars]
ansible_python_interpreter=/usr/bin/python3
